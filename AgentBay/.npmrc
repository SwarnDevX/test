shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
# Required on corporate networks with SSL inspection that blocks pkg.pr.new
# (used by some transitive deps like viem's ox dependency).
# Remove this if working on a network with full certificate trust.
strict-ssl=false
